## 1. Public contracts

- [ ] 1.1 Add `EntityRef`, `UnresolvedRef`, `ArtRef`, `PlacementRef`, and `RequirementRef` schemas to `packages/contracts/src/public/`. Verify with schema tests that a reference without `slug` or with a free-text `name`-only shape is rejected.
- [ ] 1.2 Add the shared relation row schemas: `DropRow`, `VendorRow`, `GatherRow`, `ContainerRow`, `QuestGivenRow`, `QuestRewardRow`, `QuestObjectiveRow`, `RecipeRow`, `AbilityPhase`, `FactionRewardRow`. Verify each row schema round-trips a fixture with an unresolved endpoint and with a missing optional `chance`.
- [ ] 1.3 Add per-kind document schemas `PublicItem`, `PublicNpc`, `PublicQuest`, `PublicPlace`, `PublicProperty`, `PublicAbility`, `PublicRecipe` with their typed fact blocks, and the quest objective discriminated union keyed by task type. Verify fixture documents for each kind validate and that `additionalProperties` are rejected.
- [ ] 1.4 Add the kind registry schema, the page list, the artwork resource schema, and the unified search entry schema to `StaticRootManifest`; extend `STATIC_RESOURCE_SCHEMAS` and `staticResourceEdges` so documents, lists, and artwork are graph edges. Bump `PUBLICATION_SCHEMA_VERSION` to `compendium.publication.v14`. Verify `static.test.ts` follows edges from the root to a document and an artwork resource.
- [ ] 1.5 Remove `PublicEntity.sections`, `PublicDetailRow`, `PublicDetailSection`, the `PublicAdventureGuide*`, `GuideDocument`, and `StaticGuideDocument` schemas and their registrations. Verify `bun run check` reports no remaining references outside tests slated for deletion.

## 2. Typed catalog facts and kinds

- [ ] 2.1 Add per-kind decoders in `packages/catalog/src/decoders.ts` for item, NPC, quest, scene, region, and property facts from the canonical `gameplay` payload, using the recovered `RPGItem`, `RPGNpc`, `RPGQuest`, `RPGTask`, `RPGGameScene`, and `RPGProperty` field sets. Verify decoder tests cover each enum, an out-of-range enum value that becomes a coverage issue, and a negative sentinel id that becomes an unresolved reference.
- [ ] 2.2 Add typed tables (`item_facts`, `item_stats`, `item_sockets`, `npc_facts`, `npc_stats`, `npc_ability_phases`, `npc_faction_rewards`, `quest_facts`, `quest_objectives`, `quest_rewards`, `place_facts`, `property_facts`) with provenance references in `database.ts` and populate them in `normalize.ts`. Verify `database.test.ts` inserts a fixture per kind and that reassembly is idempotent.
- [ ] 2.3 Admit abilities, effects, recipes, crafting stations, factions, currencies, skills, classes, races, enchantments, gear sets, and species as canonical kinds from the support families, with `ability_facts`, `recipe_facts`, and `recipe_materials` tables. Verify counts against the support artifact totals and that every relation id into these families resolves or is recorded as unresolved.
- [ ] 2.4 Add bidirectional relation queries to `queries.ts` (`queryDropRows`, `queryVendorRows`, `queryGatherRows`, `queryContainerRows`, `queryQuestRows`, `queryRecipeRows`, `queryContainment`). Verify `queries.test.ts` shows the same row from both endpoints for a vendor row with a progression requirement and for a boss drop row.
- [ ] 2.5 Add `artwork_assets` and `artwork_bindings` tables and ingestion of artwork evidence. Verify two entities bound to one asset produce one asset row and that an unsupported sprite record produces no binding and one coverage occurrence.

## 3. Artwork extraction

- [ ] 3.1 Read `AdventureGuidePanel`, `RPGGameScene`, `RegionTemplate`, `RPGItem`, `RPGAbility`, and `RPGNpc` in `research/recovered-types` and record which fields hold icons, portraits, and guide art in `EXPLORATION.md`. Verify each field name resolves through a HotRepl probe against the running game.
- [ ] 3.2 Add `packages/scan/src/probes/collectors/artwork.csx` that copies each referenced sprite through a `RenderTexture`, encodes PNG bytes, and returns content-addressed records with asset name, family, id, and dimensions, recording unreadable sprites with a reason. Verify a scan run against the supported build stores one PNG per item icon, per NPC portrait, per ability icon, and per guide artwork, and that the run restores runtime state.
- [ ] 3.3 Register the artwork family in the scan target envelope and the catalog admission path. Verify ingestion rejects a family whose hashes do not match stored bytes and accepts the real run.

## 4. Publication

- [ ] 4.1 Build the frozen `EntityRef` map in `packages/publication/src/references.ts` with per-kind name disambiguation and slug collision suffixes. Verify tests for two NPCs sharing a name, two items sharing a name and level, and a slug collision resolved with the native id.
- [ ] 4.2 Replace `entity-projection.ts` with per-kind projections that produce `PublicItem`, `PublicNpc`, `PublicQuest`, `PublicPlace`, `PublicProperty`, `PublicAbility`, and `PublicRecipe` from the typed facts and the relation queries. Verify projection tests show a boss drop row with equal values on the NPC and item documents and a quest objective union for each supported task type.
- [ ] 4.3 Emit one static document per entity, one list resource per kind, the kind registry, the page list, and artwork resources under `/data/art/<sha256>.png` in `resources.ts`. Verify the generated root's graph closure passes `staticResourceEdges` and that every document referenced in a list exists.
- [ ] 4.4 Add the reference audit that fails candidate selection on a reference to an unpublished entity and counts unresolved references in coverage. Verify a test with a deliberately dropped document fails selection naming the referencing document and key.
- [ ] 4.5 Regenerate the unified search corpus from the `EntityRef` map for every searchable kind with level, primary place, and placement ids; remove the separate item search shape. Verify `index-resources.test.ts` shows a quest with no placements and an NPC with placements, and that the essential-resource budget is unchanged.
- [ ] 4.6 Remove `guide-projection.ts`, `guide-resources.ts`, and their tests; emit a key-to-path map resource for legacy `?id=` links. Verify `bun test ./packages` passes and no root `guides` field remains.
- [ ] 4.7 Extend `apps/site/scripts/publication-graph.ts`, `publication-parity.ts`, and `deployment-files.ts` to verify documents, lists, artwork, and page entries, and to refuse a candidate that removes published pages or artwork. Verify `publication-parity.test.ts` covers a removed page and a removed artwork asset.

## 5. Site

- [ ] 5.1 Extend `AtlasDataLoader` with `loadDocument(kind, slug)`, `loadList(kind)`, `loadRegistry()`, and `loadCoverage()` with identity and hash checks. Verify `atlas-data.test.ts` rejects a document whose hash or build identity mismatches.
- [ ] 5.2 Add `EntityLink.svelte` that renders an `EntityRef` as an icon plus link or an `UnresolvedRef` as text, and `EntityTooltip.svelte` that loads the target document on hover or focus and closes on Escape. Verify in the browser that a link in a drop table shows the item fact card and that keyboard focus opens and closes it.
- [ ] 5.3 Add fact card components per kind and the shared relation tables (`DropTable`, `VendorTable`, `GatherTable`, `ContainerTable`, `QuestTable`, `RecipeTable`, `AbilityPhases`, `LocationList`) with `MissingValue` placeholders. Verify in the browser that an unmeasured chance renders a dash with a hover explanation and that no extra row appears.
- [ ] 5.4 Add `/[kind]/[slug]/` routes prerendered from the page list, with breadcrumb, Open Graph metadata, JSON link, build stamp, and atlas location links. Verify `SITE_STAGE=production bun run build` prerenders every listed page and that an item, an NPC, a quest, and a place page render their facts and relations in the browser.
- [ ] 5.5 Add `/[kind]/` list routes prerendered as tables from the list resource with URL-encoded sort, facets, and text filter, and a card layout on narrow screens. Verify in the browser that filtering items by slot and level, reloading, and browser back restore the same view.
- [ ] 5.6 Add the page-side search box using the unified corpus with results that open pages and offer atlas locations; switch `atlas-search.ts` to the same corpus. Verify in the browser that a boss search opens either the NPC page or the map selection and that the atlas result list still works.
- [ ] 5.7 Add `/coverage/` rendering the coverage resource. Verify in the browser that preview mode and unresolved totals appear only there.
- [ ] 5.8 Add `AtlasDetailPanel.svelte` for production: fact card, first five rows per relation, location, and page link; remove the `no-details` gating and keep `AtlasDevelopmentDetails` behind `dev`. Verify in a production build that selecting a vendor shows conditional stock, a page link, and no authoring or evidence control, and that back from the page restores the selection.
- [ ] 5.9 Remove `/guide` routes, `GuideBrowser.svelte`, `GuideBossDetails.svelte`, and `guide-load.ts`; add `_redirects` entries for section paths and prerendered legacy pages that resolve `?id=` through the key-to-path map. Verify `/guide/bosses/?id=<key>` reaches the NPC page and `/guide/regions/` reaches `/places/`.
- [ ] 5.10 Add keyboard and narrow-screen checks for pages, lists, tooltips, and the production panel. Verify the search-to-page-to-map journey without a pointer and at a 360 px viewport.

## 6. Verification and handoff

- [ ] 6.1 Run scan, catalog, and publish against the fresh selected store with artwork; stage, build, and preview. Verify the deploy smoke test requests one page per kind, one list, the coverage page, and one redirect, and record page count, build time, search bytes, and largest document bytes in `EXPLORATION.md`.
- [ ] 6.2 Rehearse rollback with `verify:deployment` between the new and previous publication roots. Verify both pass graph verification and the parity gate reports the removed guide resources as expected.
- [ ] 6.3 Update `README.md` for the compendium routes, the coverage page, and the artwork evidence; mark task 6a.10 in `openspec/changes/build-screenshot-first-map/tasks.md` as superseded by this change. Verify the documented commands reproduce the artifact set from a fresh local run.
