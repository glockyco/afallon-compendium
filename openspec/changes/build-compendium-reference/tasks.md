## 1. Public contracts

- [x] 1.1 Add `EntityRef`, `UnresolvedRef`, `ArtRef`, `PlacementRef`, and `RequirementRef` schemas to `packages/contracts/src/public/`. Verify with schema tests that a free-text `name`-only shape is rejected. `slug` is optional in the schema because page-less kinds carry none; the graph audit rejects a paged-kind reference without a slug.
- [x] 1.2 Add the shared relation row schemas: `DropRow`, `VendorRow`, `GatherRow`, `ContainerRow`, `QuestGivenRow`, `QuestRewardRow`, `QuestObjectiveRow`, `RecipeRow`, `AbilityPhase`, `FactionRewardRow`. Verify each row schema round-trips a fixture with an unresolved endpoint and with a missing optional `chance`.
- [x] 1.3 Add per-kind document schemas `PublicItem`, `PublicNpc`, `PublicQuest`, `PublicPlace`, `PublicProperty`, `PublicAbility`, `PublicRecipe` with their typed fact blocks, and the quest objective discriminated union keyed by task type. Verify fixture documents for each kind validate and that `additionalProperties` are rejected.
- [x] 1.4 Add the kind registry schema, the page list, the artwork resource schema, and the unified search entry schema to `StaticRootManifest`; extend `STATIC_RESOURCE_SCHEMAS` and `staticResourceEdges` so documents, lists, and artwork are graph edges. Bump `PUBLICATION_SCHEMA_VERSION` to `compendium.publication.v14`. Verify `static.test.ts` follows edges from the root to a document and an artwork resource.
- [x] 1.5 Remove the replaced public schemas outright: `PublicEntity`, `PublicDetailRow`, `PublicDetailSection`, `PublicEntitySummary`, `PublicItemSummary`, `PublicItemSource`, the `PublicAdventureGuide*` and `PublicGuide*` records, `GuideDocument`, `StaticGuideDocument`, `StaticEntityDetail`, `StaticEntitySearch`, `StaticItemSearch`, `StaticItemSource`, and the v2 root, with their registrations and graph edges. `PublicationData` keeps only the atlas's composed model at `compendium.publication.v14`. Verify the contracts package type-checks and its tests pass.

## 2. Typed catalog facts and kinds

- [x] 2.1 Add per-kind decoders in `packages/catalog/src/decoders.ts` for item, NPC, quest, scene, region, and property facts from the canonical `gameplay` payload, using the recovered `RPGItem`, `RPGNpc`, `RPGQuest`, `RPGTask`, `RPGGameScene`, and `RPGProperty` field sets. Verify decoder tests cover each enum, an out-of-range enum value that becomes a coverage issue, and a negative sentinel id that becomes an unresolved reference.
- [x] 2.2 Add typed tables (`item_facts`, `item_stats`, `item_sockets`, `npc_facts`, `npc_stats`, `npc_ability_phases`, `npc_faction_rewards`, `quest_facts`, `quest_objectives`, `quest_rewards`, `place_facts`, `property_facts`) with provenance references in `database.ts` and populate them in `normalize.ts`. Verify `database.test.ts` inserts a fixture per kind and that reassembly is idempotent.
- [x] 2.3 Admit abilities, effects, recipes, crafting stations, factions, currencies, skills, classes, races, enchantments, gear sets, and species as canonical kinds from the support families, with `ability_facts`, `recipe_facts`, and `recipe_materials` tables. Verify counts against the support artifact totals and that every relation id into these families resolves or is recorded as unresolved.
- [x] 2.4 Add bidirectional relation queries to `queries.ts` (`queryDropRows`, `queryVendorRows`, `queryGatherRows`, `queryContainerRows`, `queryQuestRows`, `queryRecipeRows`, `queryContainment`). Verify `queries.test.ts` shows the same row from both endpoints for a vendor row with a progression requirement and for a boss drop row.
- [x] 2.5 Add `artwork_assets` and `artwork_bindings` tables and ingestion of artwork evidence. Verify two entities bound to one asset produce one asset row and that an unsupported sprite record produces no binding and one coverage occurrence.

## 3. Artwork extraction

- [x] 3.1 Read `AdventureGuidePanel`, `RPGGameScene`, `RegionTemplate`, `RPGItem`, `RPGAbility`, and `RPGNpc` in `research/recovered-types` and record which fields hold icons, portraits, and guide art in `EXPLORATION.md`. Verify each field name resolves through a HotRepl probe against the running game.
- [x] 3.2 Add `packages/scan/src/probes/collectors/artwork.csx` that copies each referenced sprite through a `RenderTexture`, encodes PNG bytes, and returns content-addressed records with asset name, family, id, and dimensions, recording unreadable sprites with a reason. Verify a scan run against the supported build stores one PNG per item icon, per NPC portrait, per ability icon, and per guide artwork, and that the run restores runtime state.
- [x] 3.3 Register the artwork family in the scan target envelope and the catalog admission path. Verify ingestion rejects a family whose hashes do not match stored bytes and accepts the real run.

## 4. Publication

- [x] 4.1 Build the frozen `EntityRef` map in `packages/publication/src/references.ts` with per-kind name disambiguation and slug collision suffixes. Verify tests for two NPCs sharing a name, two items sharing a name and level, and a slug collision resolved with the native id.
- [x] 4.2 Replace `entity-projection.ts` with per-kind projections that produce `PublicItem`, `PublicNpc`, `PublicQuest`, `PublicPlace`, `PublicProperty`, `PublicAbility`, and `PublicRecipe` from the typed facts and the relation queries. Verify projection tests show a boss drop row with equal values on the NPC and item documents, a quest objective union for each supported task type, a place carrying its map space and region ids, and an item carrying no locations.
- [x] 4.3 Emit one static document per entity, partitioned list parts per paged kind, the kind registry, and artwork resources under `/data/art/<sha256>.webp` in `resources.ts`. Verify the generated root's graph closure passes `staticResourceEdges` and that every document referenced in a list exists.
- [x] 4.4 Add the reference audit that fails candidate selection on a reference to an unpublished entity and counts unresolved references in coverage. Verify a test with a deliberately dropped document fails selection naming the referencing document and key.
- [x] 4.5 Regenerate the unified search corpus from the `EntityRef` map for every searchable kind with level, primary place, and a `hasPlacements` flag; remove the separate item search shape. Verify `index-resources.test.ts` shows `hasPlacements: false` for a quest and `hasPlacements: true` for an NPC, and that the essential-resource budget is unchanged.
- [x] 4.6 Remove `guide-projection.ts`, `guide-resources.ts`, and their tests. No key-to-path resource is emitted because the guide URLs are gone. Verify `bun test ./packages` passes and no root `guides` field remains.
- [x] 4.7 Extend `apps/site/scripts/publication-graph.ts`, `publication-parity.ts`, and `deployment-files.ts` to verify documents, lists, artwork, and page entries, and to refuse a candidate that removes published pages or artwork. Verify `publication-parity.test.ts` covers a removed page and a removed artwork asset.

## 5. Site

- [x] 5.1 Extend `AtlasDataLoader` with `loadDocument(kind, slug)`, `loadList(kind)`, `loadRegistry()`, and `loadCoverage()` with identity and hash checks. Verify `atlas-data.test.ts` rejects a document whose hash or build identity mismatches.
- [x] 5.2 Add `EntityLink.svelte` that renders an `EntityRef` as an icon plus link or an `UnresolvedRef` as text, and `EntityTooltip.svelte` that loads the target document on hover or focus and closes on Escape. Verify in the browser that a link in a drop table shows the item fact card and that keyboard focus opens and closes it.
- [x] 5.3 Add fact card components per kind and the shared relation tables (`DropTable`, `VendorTable`, `GatherTable`, `ContainerTable`, `QuestTable`, `RecipeTable`, `AbilityPhases`, `LocationList`) with `MissingValue` placeholders. Verify in the browser that an unmeasured chance renders a dash with a hover explanation and that no extra row appears.
- [x] 5.4 Add `/[kind]/[slug]/` routes prerendered from the search corpus, with breadcrumb, Open Graph metadata, JSON link, build stamp, and atlas location links. Verify `SITE_STAGE=production bun run build` prerenders every listed page and that an item, an NPC, a quest, and a place page render their facts and relations in the browser.
- [x] 5.5 Add `/[kind]/` list routes prerendered as tables from the list resource with URL-encoded sort, facets, and text filter, and a card layout on narrow screens. Verify in the browser that filtering items by slot and level, reloading, and browser back restore the same view.
- [x] 5.6 Add the page-side search box using the unified corpus with results that open pages and offer atlas locations; switch `atlas-search.ts` to the same corpus. Verify in the browser that a boss search opens either the NPC page or the map selection and that the atlas result list still works.
- [x] 5.7 Add `/coverage/` rendering the coverage resource. Verify in the browser that preview mode and unresolved totals appear only there.
- [x] 5.8 Add `AtlasDetailPanel.svelte` for production: fact card, first five rows per relation, location, and page link; remove the `no-details` gating and keep `AtlasDevelopmentDetails` behind `dev`. Verify in a production build that selecting a vendor shows conditional stock, a page link, and no authoring or evidence control, and that back from the page restores the selection.
- [x] 5.9 Delete the guide surface: `apps/site/src/routes/guide/**`, `GuideBrowser.svelte`, `GuideBossDetails.svelte`, `guide-load.ts`, and every redirect or `?id=` resolver. No redirect file and no moved-page notice remain. Verify `/guide/` and `/guide/bosses/` return the not-found page and that the not-found page links to the map and search.
- [x] 5.10 Add place state to the atlas: a canonical URL parameter that selects a place, fits its map space, and outlines its region areas, with browser back and forward restoring it. Verify in the browser from a place page's atlas link and from a pasted URL.
- [x] 5.11 Add keyboard and narrow-screen checks for pages, lists, tooltips, and the production panel. Verify the search-to-page-to-map journey without a pointer and at a 360 px viewport.

## 6. Verification and handoff

- [x] 6.1 Run scan, catalog, and publish against the fresh selected store with artwork; stage, build, and preview. Verify the deploy smoke test requests one page per kind, one list, the coverage page, and the not-found page, and record page count, build time, search bytes, and largest document bytes in `EXPLORATION.md`.
- [x] 6.2 Rehearse rollback with `verify:deployment` between the new and previous publication roots. Verify both pass graph verification and the parity gate reports the removed guide resources as expected.
- [x] 6.3 Update `README.md` for the compendium routes, the coverage page, and the artwork evidence; mark task 6a.10 in `openspec/changes/build-screenshot-first-map/tasks.md` as superseded by this change. Verify the documented commands reproduce the artifact set from a fresh local run.

## 7. Gear sets

- [x] 7.1 Project `RPGGearSet.itemsInSet` and `gearSetTiers` in the support collector, with each tier's equipped amount and its stat rows. Verify a real scan run exports members and tiers for all 27 sets and that Adept Leather reports seven members with two tiers.
- [x] 7.2 Decode and store gear set members and tier stats in the catalog, resolving every member to an item entity and every tier stat to a stat entity, with an unresolved member recorded as a coverage issue. Verify a catalog test covers a set whose member id does not resolve, and report the real member and tier counts.
- [x] 7.3 Publish `PublicGearSet` documents, a `gear-sets` kind registry entry and list, and the owning set on each member item's facts. Verify the real publish emits 27 set documents, that Adept Leather lists its seven members and its `(3)` and `(7)` tiers, and that each member item names the set.
- [ ] 7.4 Render the gear set page and the set row on an item page in the game's tooltip order: member list, then each tier as its equipped count and its stats. Verify in the browser against `research/screenshots/reference/game-item-tooltip-armor.png`.
